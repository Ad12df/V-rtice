import 'package:flutter/material.dart';

/// Paleta de colores para la interfaz de Vértice (estética Animus / Cyber-Cartográfica).
class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0A0A0C);
  static const Color surface = Color(0xFF141419);
  static const Color surfaceElevated = Color(0xFF1C1C24);
  static const Color surfaceBorder = Color(0xFF262633);

  // Accents (Animus / Cyber-Cartography)
  static const Color cyan = Color(0xFF00F0FF);
  static const Color cyanDim = Color(0xFF00A3AD);
  static const Color cyanGlow = Color(0x3300F0FF);
  
  static const Color gold = Color(0xFFE5B842);
  static const Color goldDim = Color(0xFFA68023);
  static const Color goldGlow = Color(0x33E5B842);

  // Text & Status
  static const Color textPrimary = Color(0xFFF0F2F5);
  static const Color textSecondary = Color(0xFF8E92A0);
  static const Color textMuted = Color(0xFF5A5E6D);
  static const Color textLink = Color(0xFF00F0FF);

  // States
  static const Color error = Color(0xFFFF4D4D);
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFFB300);
}
